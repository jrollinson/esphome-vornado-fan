substitutions:
  vornado_ir_pin: GPIO4
  vornado_components_path: ".."

packages:
  vornado_fan: !include ../vornado-fan.yaml

esphome:
  name: vornado-test-esp32

esp32:
  board: esp32dev
  framework:
    type: arduino

wifi:
  ssid: "test"
  password: "test1234"
  min_auth_mode: WPA2

api:
  encryption:
    key: "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa="

ota:
  - platform: esphome
    password: "test1234"
